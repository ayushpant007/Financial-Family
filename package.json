{
  "name": "workspace",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @workspace/db run setup && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/wealth-mgmt run build && cp -r artifacts/wealth-mgmt/dist/public/* public/",
    "typecheck:libs": "echo skipping libs typecheck",
    "typecheck": "echo skipping typecheck",
    "dev": "pnpm --parallel --filter @workspace/api-server --filter @workspace/wealth-mgmt run dev"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@types/three": "^0.184.1",
    "argon2": "^0.44.0",
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  }
}
